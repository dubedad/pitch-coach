# Copy this file to .env.local and fill in the key. Never commit .env.local.
# The key stays server-side — it is only read inside the /api/critique route.
ANTHROPIC_API_KEY=

# Optional: override the model. Defaults to a current Claude Sonnet.
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
